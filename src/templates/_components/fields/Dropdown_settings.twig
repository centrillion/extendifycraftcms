{% import "_includes/forms" as forms %}

 {{ forms.selectField({
    label: "Extendify Dynamic Dropdown Options",
    instructions: "Select the Dynamic Type",
    id: "dropdownOptions",
    name: "dropdownOptions",
    options: options,
    value: field['dropdownOptions']
}) }}

<a class="fieldtoggle" data-target="advanced">Advanced</a>
<div id="advanced" class="hidden">

{% if craft.app.db.isMysql %}
{{ forms.selectField({
    label: "Column Type",
    id: 'columnType',
    name: 'columnType',
    instructions: "The type of column this field should get in the database.",
    options: [
        { value: 'string', label: 'varchar (255B)' },
        { value: 'text', label: 'text (~64KB)' },
        { value: 'mediumtext', label: 'mediumtext (~16MB)' },
    ],
    value: field.columnType,
    warning: (field.id ? "Changing this may result in data loss."),
}) }}
{% endif %} 

</div>
